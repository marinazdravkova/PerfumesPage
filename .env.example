# Example environment variables for the Perfume Store project
# Copy this file to `.env.local` (or `.env`) and edit values as needed.

# Backend API base URL used by the frontend to contact the API
REACT_APP_API_URL=https://localhost:7009

# Other env variables can go here
# REACT_APP_OTHER_KEY=value
